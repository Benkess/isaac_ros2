BootStrap: docker
From: osrf/ros:humble-desktop-full-jammy

%labels
    Maintainer mxk3rz
    Description multi-user ROS 2 Humble container for Isaac Sim integration
    Version v1.0.3

%post
    # Basic utilities and ROS tooling
    apt-get update && apt-get install -y --no-install-recommends \
        git \
        nano \
        curl \
        lsb-release \
        gnupg2 \
        locales \
        python3-pip \
        python3-venv \
        python3-colcon-common-extensions \
        python3-rosdep \
        build-essential \
        ros-humble-vision-msgs \
        ros-humble-ackermann-msgs \
        --no-install-recommends \
        && apt-get clean && rm -rf /var/lib/apt/lists/* \
        rm -rf /var/cache/debconf/*

    locale-gen en_US en_US.UTF-8
    update-locale LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8

    # Ensure dpkg's backup file is deleted before image finalization
    rm -f /var/lib/dpkg/status-old || true

    # Optional rosdep setup: 
    # rosdep init || true
    # rosdep update || true
